import Testing
@testable import EmpDesignSystem

struct EmpDesignSystemTests {
    @Test
    func example() {
        // Write your test here and use APIs like `#expect(...)` to check expected conditions.
    }
}
